name: field_array_picker

version: 0.1.0

dependencies:
  uuid: ^3.0.1
  intl: ^0.17.0
  flutter_picker: ^2.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.5.0 <3.0.0"